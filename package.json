{
  "name": "reader",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build && vite build --mode background && vite build --mode script",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.32",
    "typescript": "~5.9.3",
    "vite": "^7.2.4"
  }
}
